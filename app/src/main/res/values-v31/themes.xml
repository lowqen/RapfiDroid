<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 12+ shows a splash screen whether an app asks for one or not: with
      no attributes set it uses the launcher icon on the window background,
      which for this app meant the old flat 108dp board shrunk into a circle.
      These four lines are the whole opt-in — no library, so the offline
      dependency cache is untouched.
    -->
    <style name="Theme.YixinDroid" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/launch_icon_background</item>
    </style>
</resources>
