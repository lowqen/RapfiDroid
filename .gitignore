*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build/
.kotlin/

# Signing: the keystore and its passwords are the user's, never the repo's.
keystore.properties
*.jks
*.keystore

# Release build output (Android Studio writes the signed APK here).
/app/release/

# JVM/Gradle crash dumps left behind by an OOM'd daemon.
hs_err_pid*.log
replay_pid*.log

# OS/cloud-sync clutter (Google Drive scatters this into every folder).
desktop.ini
Thumbs.db
